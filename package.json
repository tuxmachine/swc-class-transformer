{
  "name": "webcontainers-api-starter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "rm -Rf dist && swc -d dist index.ts",
    "start": "npm run build && node dist/index.js"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.68"
  },
  "dependencies": {
    "class-transformer": "^0.5.1"
  }
}
